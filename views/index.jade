extends layout

block content
	form(action="/vs" method="post" id="nickname_form")
		input(type="submit" id="nickname_send" value="Send")

	script.
		// connection to socket.io
		var socket = io.connect('http://localhost:3000');
		socket.emit('new_client');
		